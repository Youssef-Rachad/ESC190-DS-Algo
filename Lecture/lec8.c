// ESC190 Lecture: 8
// Date : 28/01/2021

struct Point {
    double x;
    doubly ;
};

double euclidean_dist(struct Point p1, struct Point p2);

typedef struct FancyList{
    int* ptr; // pointer to head
    int max_el; // max # of elements
    int count_el; // current # of elements
} FancyList;
// write code to initialize the list and function to append and print list contents
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char* argv[]){
  
  return 0;
}
